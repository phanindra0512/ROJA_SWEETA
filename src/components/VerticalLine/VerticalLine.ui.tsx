import { View, Text } from "react-native";
import React from "react";
import styles from "./VerticalLine.styles.ts";

const VerticalLine = () => {
  return <View style={styles.verticalLine} />;
};

export default VerticalLine;
